# AGENTS.md

## 约定

- skill 名称和目录名使用小写字母、数字和连字符。
- skill 名称优先描述能力或任务，不绑定当前实现细节、单一供应商或临时数据源，除非该外部系统本身就是 skill 的核心。
- 正式 skill 按用途放在 `skills/capabilities/<skill-name>/` 或 `skills/workflows/<skill-name>/`。
