# Skills

个人使用的 coding agent skills 仓库，用于集中管理和持续迭代可复用的能力与工作流。

## 目录

```text
skills/
├── capabilities/  # 可独立调用的能力
└── workflows/     # 多步骤工作流
```

每个 skill 以 `SKILL.md` 为入口，相关脚本、参考资料和 agent 配置放在同一目录中。
